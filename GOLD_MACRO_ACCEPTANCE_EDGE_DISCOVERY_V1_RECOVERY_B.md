# Gold Macro-Acceptance Edge Discovery V1 — Recovery B

The Recovery A execution passed its corrected source-boundary gate and completed primary and reference feature construction and statistical evaluation. It then stopped before checksum construction, artifact persistence, or result display because the canonical serializer did not convert `numpy.bool_` gate values into native JSON booleans.

No relationship result, test statistic, ranking, or candidate disposition was displayed or inspected. Preserve both previous failures and every frozen analytical definition unchanged.

Change only the output-normalization function so any `numpy.generic` scalar is converted through `.item()` before recursive rounding and JSON serialization. Do not modify source selection, anchors, features, tests, endpoints, support, random draws, statistics, multiplicity, gates, or ranking. Rerun deterministically and disclose this recovery in the final seal.
